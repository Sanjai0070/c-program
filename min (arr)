#include <stdio.h>
int main()
{
  int a[5]={1,2,3,4,5},min,i,size;
  size = sizeof(a)/sizeof(a[0]);
  min=a[0];
  for (i=0;i<size;i++)
  {
    if (a[i]<min)
    {
      min=a[i];
    }
  }
  printf("%d is the minimum number",min);
  return 0;
}
